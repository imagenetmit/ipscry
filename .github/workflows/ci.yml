name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  build-test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-go@v7
        with:
          go-version: '1.26.6'
          check-latest: true

      - name: Format check
        run: test -z "$(gofmt -l .)"

      - name: Vet
        run: go vet ./...

      - name: Test
        run: go test ./... -count=1

      - name: Vulnerability check
        run: |
          go install golang.org/x/vuln/cmd/govulncheck@latest
          govulncheck ./...

      - name: Build (linux/amd64)
        env:
          GOOS: linux
          GOARCH: amd64
        run: go build -trimpath -ldflags "-X main.appVersion=ci-${{ github.sha }}" -o dist/ipscry-linux-amd64 .

      - name: Build (windows/amd64)
        env:
          GOOS: windows
          GOARCH: amd64
        run: go build -trimpath -ldflags "-X main.appVersion=ci-${{ github.sha }}" -o dist/ipscry.exe .
